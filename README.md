# autoparser
Add an application.yml file to the main/java/resources directory. Add the following input to the file to make the parser work.

woocommerce:
base-url: "URLOFWOOCOMMERCEENDPOINT"
consumer-key: "PERSONALCONSUMERKEY"
consumer-secret: "SECREYKEY"